import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import Dog from './Dog';

ReactDOM.render(
  <React.StrictMode>
    <Dog />
  </React.StrictMode>,
  document.getElementById('root'),
);
